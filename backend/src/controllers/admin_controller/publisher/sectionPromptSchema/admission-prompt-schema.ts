import { SchemaType } from "@google/generative-ai";

const admissionPromptSchema = {
  description:
    "Describe the admission related aspect of the post",
  type: SchemaType.OBJECT,
};
export default admissionPromptSchema;
//not needed (irony)
